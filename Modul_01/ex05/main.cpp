

#include "Karen.hpp"

int main()
{


    Karen first;
    first.complain("DEBUG");
    first.complain("INFO");
    first.complain("WARNING");
    first.complain("ERROR");
    first.complain("ES");
    return (0);
}